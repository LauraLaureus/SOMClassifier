function representDataInSpace(Data,SuperposedData)

X = Data(:,1);
Y = Data(:,2);
Z = Data(:,3);

figure;
plot3(X,Y,Z,'o');
hold on 
title('Data in space');
grid on;

X2 = reshape(SuperposedData(:,:,1),[],1);
Y2 = SuperposedData(:,:,2);
Z2 = SuperposedData(:,:,3);

plot3(X,Y,Z,'dg');
hold off;
end